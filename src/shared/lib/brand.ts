/**
 * Central brand constants.
 * Change these to rebrand the entire app — UI text, page titles,
 * and storage keys all derive from this file.
 */

/** Franchise / series label shown above the product name. */
export const APP_SERIES = 'Monster Hunter';

/** Product name shown as the main heading. */
export const APP_NAME = 'Campaign Tracker';

/** Full title used in <title> tags and tab labels. */
export const APP_TITLE = `${APP_SERIES} ${APP_NAME}`;

/** localStorage key used by the auth persistence layer. */
export const AUTH_STORE_KEY = 'mh-auth';
